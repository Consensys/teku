{
  "title" : "GetGasLimitData",
  "type" : "object",
  "required" : [ "gas_limit", "pubkey" ],
  "properties" : {
    "gas_limit" : {
      "type" : "string",
      "description" : "unsigned 64 bit integer",
      "example" : "1",
      "format" : "uint64"
    },
    "pubkey" : {
      "$ref" : "#/components/schemas/Pubkey"
    }
  }
}